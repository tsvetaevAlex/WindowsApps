namespace Budgethelper.Models
{
    public enum MessageType
    {
        Info,
        Debug,
        Warn,
        DB,
        UI,
        User,
        Transaction,
        Account
    }
}
